import React from 'react';

const ManageSearchProducts = ({service}) => {
    const {img, name, category, Brand, Qty, price} = service
    console.log(service)
    return (
        <div>
            
        </div>
    );
};

export default ManageSearchProducts;